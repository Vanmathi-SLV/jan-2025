import React from 'react';

const Header = () => {
    return (
        <header style={{ padding: '10px', background: '#333', color: '#fff' }}>
            <h1>My App</h1>
        </header>
    );
};

export default Header;